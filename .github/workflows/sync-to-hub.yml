      - name: Push to hub
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: git push --force https://johnboscokibet:$HF_TOKEN@huggingface.co/spaces/johnboscokibet/deutsch-ai-tutor main
